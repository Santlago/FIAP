SET SERVEROUTPUT ON;

DECLARE

    V_ESTADO VARCHAR2(30);
    V_PAIS VARCHAR2(30);
BEGIN
    SELECT A.NOME,
           B.NOME INTO V_PAIS, V_ESTADO
    FROM PAIS A
    INNER JOIN ESTADO B
    ON (A.ID=B.ID_ESTADO);
    
    dbms_output.PUT_LINE(V_PAIS, V_ESTADO);
END;